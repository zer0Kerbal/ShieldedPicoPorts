// Changelog.cfg v1.0.0.4
// Shielded PicoPort (SPP)
// created: 01 Aug 2019
// updated: 17 Jun 2021

// ——————————————————————————————————————————————————
// ——— changelog ————————————————————————————————————
// ——————————————————————————————————————————————————

KERBALCHANGELOG
{
	showChangelog = True
	modName = Shielded PicoPort
	license = GPLv2
	author = zer0Kerbal
	VERSION
	{
		version = 1.1.0.0
		versionName = >>-- Final Countdown - for KSP 1.11.2 --<<
		change = thorough linting of configs
		change = minor corrections to readme and changelog 
		change = backend automations
		CHANGE
		{
			change = Added
			subchange = Inflatable PicoPort Basic
		}
		change = added all parts to ConnectedLivingSpaces and TweakScale patches
	}
	VERSION
	{
		version = 1.0.1.0
		versionName = >>-- Reminiaturized - for KSP 1.8.1 --<<
		change = corrected changelog
		change = added localization code
		change = removed extraneous scale =
		change = removed localization code since it just doesn't work this way. :(
		change = updated to new KERBALCHANGELOG format!
		CHANGE
		{
			change = added three new models!
			subchange = Basic, 
			subchange = Now has Female
			subchange = Now has Male
			subchange = Now has and Plus		
		}
	}
	VERSION
	{
		version = 1.0.0.2
		versionName = Needy little things
		change = [D][BUG 1.0.0.2a] part isn't loading
		change = [D][BUG 1.0.0.2a] change in folder structure was not reflected in patches :NEEDS statement which in turn caused the part to not load.
		change = remove errors in Changelog.cfg
	}
	VERSION
	{
		version = 1.0.0.1
		versionName = ReStocked and Flush
		change = added PicoPortShielded.restockwhitelist
		change = added ReStock patch (so PicoPortShielded will show up with ReStock installed)
		change = turned off PicoPortShielded.restockwhitelist
		change = adjusted placement of model so will no longer sink into part attached to
		change = moved TweakScale and ConnectedLivingSpaces patches into separate files
		change = 
	}
	VERSION
	{
		version = 1.0.0.0
		versionName = Phalanx those Ports!
		change = >>-- creation by zer0Kerbal --<<
	}
}

// GPLv2 unported
// zer0Kerbal
